# Example Agent

A demo agent showing the standard directory structure.

## Usage

Copy this directory into your project and customize the configuration files.

## Files

- `CLAUDE.md` — Agent system prompt / configuration
